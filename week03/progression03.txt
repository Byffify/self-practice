- หลักการใช้ if ตรวจสอบว่า input เป็น string และไม่ใช่ค่าว่าง ถ้าไม่ใช่ให้คืนค่า undefined
- แปลงข้อความทั้งหมดเป็นตัวพิมพ์เล็ก แล้วแยกคำด้วยช่องว่าง ด้วย .toLowerCase().split(" ")
- การเก็บจำนวนครั้งเป็น object และคืนค่า